name: flutter_todo_app_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: ^3.0.1
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  hive_generator: ^2.0.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: assets/icons/TDr.png

flutter:
  uses-material-design: true
  assets:
    - assets/icons/TDr.png
